/* global Swiper*/
document.addEventListener('DOMContentLoaded', function() {      // 発火タイミングを指定
  const swiper = new Swiper('.swiper', {                        // 初期化
                 // オプションを記述
                 loop: true,
                // ループを許可
  })
});